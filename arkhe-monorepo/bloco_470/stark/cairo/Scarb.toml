# =============================================================================
# BLOCO 470 v13/v15 — PROJETO CAIRO (O4/S1)
# Scarb.toml
# Projeto Cairo para circuitos de governança provados via stone-cli (zk-STARK).
# =============================================================================
[package]
name = "governance_stark"
version = "0.1.0"
edition = "2024_07"

[dependencies]
starknet = "2.8.0"

[cairo]
# Caminho para o diretório de fontes (padrão Scarb: src/)

[scripts]
test = "scarb test"
build = "scarb build"
prove = "stone-cli prove --program target/release/governance_stark.casm.json"
verify = "stone-cli verify --proof proof.json"