[package]
name = "arkhe-oam"
version = "0.1.0"
edition = "2021"
description = "ARKHE OAM — pureza da carga de momento angular orbital (#10) para o z-puro photônico"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
anyhow = "1"

[lints.rust]
unsafe_code = "deny"