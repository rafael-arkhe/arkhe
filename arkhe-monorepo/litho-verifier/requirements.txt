# Litho Verifier v3.1.0 — Dependencies
# Core (stdlib only for litho_verifier)
# No external deps for the verifier itself

# API Server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Optional: for updater web scraping
beautifulsoup4>=4.12.0
lxml>=5.0.0
requests>=2.31.0

# Optional: for advanced testing
pytest>=8.0.0
pytest-cov>=4.1.0
httpx>=0.27.0

# Optional: for PDF report generation
markdown>=3.5.0
weasyprint>=61.0